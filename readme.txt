US- To run the program correctly, both files must be in the same folder. By default the games file directory should be: "E:\games.dat".
Otherwise, you can change the directory on line 1814. By replacing the old text with the new address.

ES- Para ejecutar correctamente el programa, ambos archivos deben estar en la misma carpeta. Por defecto el directorio del archivo partidas debería ser:  "E:\partidas.dat". 
De lo contrario, puede cambiar el directorio en la línea 1814. Sustituyendo el texto anterior por la nueva dirección. 
